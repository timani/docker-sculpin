---
driver:
  name: digitalocean

platforms:
  - name: ubuntu-14.04

provisioner:
  name: ansible_playbook

verifier:
  # Alternatively for RVM
  # which ruby
  # ~/.rvm/rubies/ruby-2.2.0/bin/ruby
  ruby_bindir: '~/.rvm/rubies/ruby-2.2.0/bin/ruby'

platforms:
  - name: ubuntu-14

suites:
  - name: sculpin
